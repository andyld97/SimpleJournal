<Window x:Class="SimpleJournal.Dialogs.UpdateDownloadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleJournal.Dialogs"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Downloading ..." SizeToContent="Height" Width="500" Background="{DynamicResource WhiteBrush}">
    <StackPanel Margin="2">
        <StackPanel.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Margin" Value="2" />
                <Setter Property="Padding" Value="2" />
                <Setter Property="Foreground" Value="{DynamicResource BlackBrush}" />
            </Style>
        </StackPanel.Resources>
        <TextBlock FontWeight="Bold">
            <Run Text="Downloading SimpleJournal v" /><Run x:Name="RunVersion" />
            <Run Text="..." />
        </TextBlock>
        <ProgressBar x:Name="ProgressDownload" Margin="4,0,2,0" Height="15" />
        <TextBlock FontWeight="Bold">
            <Run Text="Speed: " />
            <Run x:Name="RunSpeed" />
        </TextBlock>
    </StackPanel>
</Window>
